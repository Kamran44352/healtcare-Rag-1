fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart
pydantic>=2.7
pydantic-settings>=2.3
supabase>=2.5
qdrant-client[fastembed]>=1.9
openai>=1.35
llama-parse>=0.5
tiktoken>=0.7
httpx>=0.27
tenacity>=8.3
python-dotenv>=1.0
cachetools>=5.3
redis>=5.0
certifi>=2024.0.0
pymupdf>=1.24.0
# Agent + observability. langsmith and langchain-core were already installed as
# langgraph transitives and are imported directly (RunnableConfig, @traceable),
# so they're declared explicitly rather than left to resolve by accident.
langgraph>=1.1
langchain-core>=1.3
langsmith>=0.8
sse-starlette>=2.0
